class AluguelDeCarro {
    carroAluguel(cidade, data) {
      console.log(`Aluguel de carro em ${cidade} 
        para a data ${data} foi confirmado.`);
      return true;
    }
  }
  
  module.exports = AluguelDeCarro;
